import { PrixCategoryPipe } from './prix-category.pipe';

describe('PrixCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new PrixCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
